@using Microsoft.AspNetCore.Identity
@model Thrift_Us.ViewModel.OrderDetailsViewModel
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
<form method="post">
    <input type="hidden" asp-for="OrderHeader.Id" />
    <div class="container-sm">
        <div class="row">
            <div class="col-6">
                <h2>Pickup Details</h2>
                
                    @if (SignInManager.IsSignedIn(User))
                    {
                        var user = await UserManager.GetUserAsync(User);
                        var roles = await UserManager.GetRolesAsync(user);
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.Name">Name</label>
                            <input asp-for="OrderHeader.Name" class="form-control" />
                            <span asp-validation-for="OrderHeader.Name" class="text-danger"></span>
                        }
                        @if(roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.Name"> Name</label>
                            <input readonly asp-for="OrderHeader.Name" class="from-control"/>
                        }
                        
                     </div>
                        <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.Phone">Phone</label>
                            <input asp-for="OrderHeader.Phone" class="form-control" />
                            <span asp-validation-for="OrderHeader.Phone" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.Phone">Phone</label>
                            <input readonly asp-for="OrderHeader.Phone" class="from-control" />
                        }

                        </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.Address">Address</label>
                            <input asp-for="OrderHeader.Address" class="form-control" />
                            <span asp-validation-for="OrderHeader.Address" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.Address">Address</label>
                            <input readonly asp-for="OrderHeader.Address" class="from-control" />
                        }

                    </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.City">City</label>
                            <input asp-for="OrderHeader.City" class="form-control" />
                            <span asp-validation-for="OrderHeader.Address" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.City">City</label>
                            <input readonly asp-for="OrderHeader.City" class="from-control" />
                        }

                    </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.State">State</label>
                            <input asp-for="OrderHeader.State" class="form-control" />
                            <span asp-validation-for="OrderHeader.State" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.State">State</label>
                            <input readonly asp-for="OrderHeader.State" class="from-control" />
                        }

                    </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.PostalCode">PostalCode</label>
                            <input asp-for="OrderHeader.PostalCode" class="form-control" />
                            <span asp-validation-for="OrderHeader.PostalCode" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.PostalCode">PostalCode</label>
                            <input readonly asp-for="OrderHeader.PostalCode" class="from-control" />
                        }

                     </div>
                     <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller")|| roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.ApplicationUser.Email">Email</label>
                            <input readonly asp-for="OrderHeader.ApplicationUser.Email" class="form-control" />

                        }
                       
                     </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller")|| roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.OrderDate">OrderDate</label>
                            <input readonly asp-for="OrderHeader.OrderDate" class="form-control" />

                        }

                    </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.PaymentStatus">PaymentStatus</label>
                            <input asp-for="OrderHeader.PaymentStatus"  class="form-control" />
                            <select asp-for="OrderHeader.PaymentStatus" asp-items="ViewBag.PaymentStatus" class="form-control"></select>
                           <span asp-validation-for="OrderHeader.PaymentStatus" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.PaymentStatus">PaymentStatus</label>
                            <input readonly asp-for="OrderHeader.PaymentStatus" class="from-control" />
                        }


                    </div>
                    <div class="row mb-3">
                        @if ((user.UserName=="admin0@gmail.com") || roles.Contains("Seller"))
                        {
                            <label asp-for="OrderHeader.OrderStatus">OrderStatus</label>
                            <input asp-for="OrderHeader.OrderStatus" class="form-control" />
                            <select asp-for="OrderHeader.OrderStatus" asp-items="ViewBag.OrderStatus" class="form-control"></select>
                            <span asp-validation-for="OrderHeader.OrderStatus" class="text-danger"></span>
                        }
                        @if (roles.Contains("Buyer"))
                        {
                            <label asp-for="OrderHeader.OrderStatus">OrderStatus</label>
                            <input readonly asp-for="OrderHeader.OrderStatus" class="from-control" />
                        }


                    </div>
                    
                 }
                <div class="row mb-3">
                    <button class="submit" value="Update Order">Update Order</button>

                </div>
               
            </div>
         @*   <div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0">Summary</h5>
                    </div>
                    <div class="card-body">
                        <ul class="list-group list-group-flush">
                            @foreach(var item in Model.OrderDetails)
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-centre">
                                    <span>@item.Product.ProductName</span>
                                    <span>QTY:@item.Count</span>
                                    <span>Price:@item.Product.Price</span>
                                </li>
                            }
                            </ui>

                    </div>
                </div>
             </div>*@
    </div>
</form>
